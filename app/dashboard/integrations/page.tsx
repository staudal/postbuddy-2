import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Integrationer",
  description: "Integrationer",
};

export default function IntegrationsPage() {
  return <p>Integrationer</p>;
}
